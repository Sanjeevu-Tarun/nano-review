{
  "name": "nanoreview-api",
  "version": "3.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install:deps": "npm install && npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.3.1",
    "fastify": "^5.7.3",
    "got-scraping": "^4.0.4",
    "tls-client": "^1.3.1",
    "playwright": "^1.58.2"
  }
}
